#include <bits/stdc++.h>

using namespace std;

int main() {
    int  a ,b ,c , d ;
    
    cout << "Ingresa 3 numeros diferentes entre el 1 y 50 :";
    cin >> a, b, c ;
    
    if (a >= b, a >= c)

      d = a *2 + b + c ;
      
    
    else (b >= a, b >= c);
     
     d = b * 2 + a + c;
     
     
    if (c <= a , c <= b)
     
     d = c*2 + b + a;
     
    bool end;
    
    
    cin>> d ;

    cout << "El valor mayor es "<< d;

    return 0;}