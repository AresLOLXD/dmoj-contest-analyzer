#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

int main() {
int n;
if (!(cin >>n)) return 0

string s;

vector<int> derecha(26, 0); 
vector<bool> izquierda(26, false);
int max_coincidencias=0;
for (int i =0; i< n -1; ++i){
    int idx =s[i] - 'a';
}
}