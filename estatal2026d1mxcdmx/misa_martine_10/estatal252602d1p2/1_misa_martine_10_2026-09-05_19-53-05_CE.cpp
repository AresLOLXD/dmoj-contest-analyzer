#include <iostream>
using namespace std;

int main() {

    int n;
    int arreglo[n];
    int posicion;
    int posicion1 = 0;
    int posicion2 = 0;

    for(int i = 0; i > n; i++){
        cin >> arreglo[i];

    posicion1 = (posicion n+1) + (posicion n+2);
    posicion2 = (posicion n-1) + (posicion n-2);
    }
    cout << posicion;
    return 0;
    }