#include <iostream>

using namespace std;

int main() {
        
    int numero;
    
    int A;
    int B;
    int C;
    int K = ((A * 2) + B + C);
    
    std::cin >> numero;
    
    K = ((A * 2) + B + C);
    if (K >= 1 and K <= 30);
    
    std::cin >> A >> B >> C;
    
    return 0;
}